{
  "name": "plexus",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "macOS HUD that shows coding agents walking a repo in real time",
  "scripts": {
    "start": "go run ./cmd/plexus",
    "hud": "go run ./cmd/plexus",
    "build:hud": "swift build -c release --package-path macos && go run ./cmd/plexus package-hud",
    "quit": "go run ./cmd/plexus quit",
    "test": "go test ./... && node --test tests/layout.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.web.json"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
